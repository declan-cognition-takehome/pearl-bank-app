# @pearl/scheduled-payments

Owned by **Payments**.

Recurring payments at `/scheduled-payments`. Builds directly on `@pearl/payments`
(`PaymentsService`, draft helpers) and `@pearl/shared-design-system`, so it can only be
migrated after `payments`.

```bash
npm run test:scheduled-payments
npm run lint:scheduled-payments
```
