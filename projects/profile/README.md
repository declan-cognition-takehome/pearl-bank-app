# @pearl/profile

Owned by **Customer Profile**.

Contact-details page at `/profile` with inline field editing. Depends on
`@pearl/shared-design-system` and `@pearl/shared-analytics`. Exports `ProfileFieldComponent`,
which `@pearl/account-settings` reuses.

```bash
npm run test:profile
npm run lint:profile
```
