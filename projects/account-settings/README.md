# @pearl/account-settings

Owned by **Customer Profile**.

Alerts and notification preferences at `/settings`. Reuses `ProfileFieldComponent` and
`ProfileService` from `@pearl/profile`, plus `@pearl/shared-design-system`, so it can only be
migrated after `profile`.

```bash
npm run test:account-settings
npm run lint:account-settings
```
