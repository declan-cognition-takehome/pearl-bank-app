# @pearl/shared-analytics

Owned by **Digital Analytics**.

`AnalyticsService` buffers page views and product events for `accounts`, `payments`
and `profile`. Nothing leaves the browser in this workspace.

```bash
npm run test:shared-analytics
npm run lint:shared-analytics
```
