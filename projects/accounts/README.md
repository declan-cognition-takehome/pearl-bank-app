# @pearl/accounts

Owned by **Accounts**.

Account overview at `/accounts`: balances per account, total balance and recent transactions
grouped by day. Depends on `@pearl/shared-design-system`, `@pearl/shared-auth` (scope
`accounts:read`) and `@pearl/shared-analytics`.

```bash
npm run test:accounts
npm run lint:accounts
```
